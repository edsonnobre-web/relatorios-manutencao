const OWNER={id:'owner',name:'Proprietário do Sistema',login:'proprietario',password:'1234',role:'Proprietário',isOwner:true};let currentUser=null;function ensureAdmin(){const u=read(DB.users,[]);if(!u.some(x=>x.login==='admin')){u.push({id:uid(),name:'Administrador',login:'admin',password:'1234',role:'Administrador'});write(DB.users,u)}}function setMsg(m,t='error'){const e=document.getElementById('loginMessage');e.textContent=m||'';e.className=m?t:''}function login(){const l=document.getElementById('loginUser').value.trim(),p=document.getElementById('loginPass').value;if(!l||!p)return setMsg('Preencha o usuário e a senha.');const u=(l===OWNER.login&&p===OWNER.password)?OWNER:read(DB.users,[]).find(x=>x.login===l&&x.password===p);if(!u)return setMsg('Usuário ou senha inválidos.');currentUser={...u};sessionStorage.setItem('rm_session',JSON.stringify(currentUser));setMsg('Acesso autorizado.','ok');openApp()}function logout(){sessionStorage.removeItem('rm_session');currentUser=null;document.getElementById('appScreen').classList.add('hidden');document.getElementById('loginScreen').classList.remove('hidden');document.getElementById('logoutBtn').classList.add('hidden');document.getElementById('userInfo').textContent=''}function openApp(){document.getElementById('loginScreen').classList.add('hidden');document.getElementById('appScreen').classList.remove('hidden');document.getElementById('logoutBtn').classList.remove('hidden');document.getElementById('userInfo').textContent=currentUser.name+' — '+currentUser.role;document.querySelectorAll('.adminOnly').forEach(e=>e.classList.toggle('hidden',!['Administrador','Proprietário'].includes(currentUser.role)));refreshAll();showTab('dashboard')}